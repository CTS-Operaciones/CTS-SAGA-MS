import { STATUS_ENTRIES } from "../constants";

export interface filter {
  model: number;
  brand: number;
  ubication: number;
  name: string;
  clasification: number;
  resource: number;
  user_id: number;
  status: STATUS_ENTRIES;
}
  
 